@model IEnumerable<Cadastro.TB_Grupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sigla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Area.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Empresa.RazaoSocial)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sigla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Area.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Empresa.RazaoSocial)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdGrupo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdGrupo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdGrupo })
        </td>
    </tr>
}

</table>
